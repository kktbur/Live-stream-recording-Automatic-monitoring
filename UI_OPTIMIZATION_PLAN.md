# Reco Box UI 优化计划

## 目标

在不改变录制、监控、数据库、预览、导入和托盘行为的前提下，只优化现有
PySide6 + Qt Quick/QML 界面的视觉层级、交互反馈、可读性和一致性。

## 固定约束

- 保留 Python 3.12、PySide6、Qt Quick Controls 和 QML 技术栈。
- 不引入 HTML、CSS、Tailwind、React、WPF、WinUI 或新的第三方 UI 依赖。
- 不新增业务功能、页面或设置项，不改模型、控制器和数据库接口。
- 不使用复杂动画；只保留即时的悬停、按下、焦点和禁用反馈。
- 不为了视觉效果增加大体积图片、字体或运行时资源。
- 录制中使用绿色语义；危险操作使用红色语义；其余保持黑白中性品牌基调。
- 修改集中在 `src/reco_box/ui/Main.qml`，通过少量可复用组件减少重复样式。

## 设计方向

- 风格：克制、专业、扁平、工具型桌面控制台。
- 信息密度：保留卡片式高效浏览，但用间距和字重建立层级。
- 动效：低动效，状态变化不移动布局边界。
- 尺寸：主要图标点击区域至少 44×44；主要按钮高度统一。
- 可访问性：图标按钮保留明确提示和可访问名称；键盘焦点可见；状态不只靠颜色表达。
- 颜色：统一使用语义令牌，减少散落的十六进制颜色。

## 实施步骤

1. 在 `ApplicationWindow` 中集中定义背景、表面、文字、边框、主操作、成功、警告、危险和焦点颜色。
2. 完善并复用 `ToolIconButton`，新增轻量 `AppButton`，统一高度、圆角、字重、悬停、按下、焦点和禁用状态。
3. 优化顶部工具栏：保持现有功能和顺序，突出添加、全部开始，区分暂停、删除和设置；筛选、排序、搜索保持紧凑。
4. 优化直播卡片：加强主播、链接、状态、录制参数和错误信息层级；统一操作按钮主次关系，不增加卡片内容。
5. 优化空状态和操作反馈：提供明确的下一步，但不增加新的通知系统。
6. 优化现有添加、编辑、删除、全局设置、历史、日志、预览和导入对话框的表面、分隔、主要操作和危险操作层级。
7. 运行 QML 离屏加载测试和完整测试；检查仅有 QML/计划文件发生变化。
8. 生成新预览图；确认后再构建 Windows 安装包，不覆盖用户现有数据库和录制文件。

## 明确不做

- 不加入深色模式、皮肤市场、主题编辑器或动画库。
- 不重构录制和监控流程。
- 不更改现有字段、按钮语义和菜单结构。
- 不为追求差异化加入装饰性插画、渐变、玻璃效果或大面积阴影。
- 不拆分大量 QML 文件；本轮先用少量内联复用组件控制复杂度。

## 验收标准

- 应用能够正常启动并加载 `Main.qml`。
- 添加、全部开始、全部暂停、删除全部、设置、筛选、排序和搜索入口仍然存在。
- 编辑窗口仍保持固定尺寸和内部滚动。
- 所有原有自动化测试通过。
- 不新增运行时依赖，安装包体积不会因本轮 UI 调整出现实质增长。
- Python 业务文件无改动。
