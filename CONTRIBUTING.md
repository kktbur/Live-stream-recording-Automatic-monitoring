# Contributing to Reco Box

感谢你愿意帮助改进 Reco Box。项目当前面向 Windows x64，重点是匿名直播监控、稳定录制、可恢复性和清晰的本地桌面体验。

## 提交 Issue

提交前请先搜索现有 Issues，并确认问题仍能在最新 Release 或 `main` 分支复现。普通问题请包含：

- Reco Box 版本或提交号；
- Windows 版本；
- 涉及的平台；
- 可公开访问的直播间地址；
- 预期行为、实际行为和最短复现步骤；
- 已脱敏的运行日志或截图。

不要提交 Cookie、账号令牌、代理凭据、带签名的临时直播流地址、私人直播间地址、录制视频或其他个人数据。

## 报告平台失效

标题建议使用：`[Platform] 平台名：问题摘要`。

除普通 Issue 信息外，请补充：

- 直播间当时是否正在直播；
- 浏览器中是否能匿名打开；
- 是无法识别平台、无法获取直播状态、无法获取播放地址，还是 FFmpeg 录制失败；
- 问题首次出现的大致日期；
- 同平台是否有另一个公开直播间可以复现。

平台网页和接口经常变化。仅报告“某平台失效”而没有公开示例、时间和错误阶段，通常无法可靠复现。

## 提交 Pull Request

1. Fork 仓库并从最新 `main` 创建一个范围明确的分支。
2. 一个 PR 只解决一个问题，避免同时重构无关模块。
3. 为行为变化增加或更新测试。
4. 本地运行：

   ```powershell
   ./.venv/Scripts/python.exe -m ruff check src tests tools
   $env:QT_QPA_PLATFORM='offscreen'
   ./.venv/Scripts/python.exe -m pytest tests -q -p no:cacheprovider
   ```

5. 在 PR 描述中写明问题、实现方式、验证结果和已知限制；UI 变化请附截图。

不要在 PR 中提交构建产物、数据库、日志、Cookie、录制文件或 FFmpeg 二进制。`vendor/DouyinLiveRecorder/` 应保持与记录的上游版本一致；同步上游或应用必要补丁时，请单独提交并记录来源、版本、许可证和差异。

## 提交新平台适配

新平台适配必须遵守以下边界：

- 只支持匿名、公开可访问的直播内容；
- 不绕过登录、付费、地区限制、访问控制或平台风控；
- 不硬编码私人 Cookie、App Secret、访问令牌或代理凭据；
- 说明使用的公开接口、上游代码和许可证来源；
- 记录请求头、TLS 校验、重定向、代理和临时播放地址的安全行为；
- 在平台识别、解析成功、未开播、匿名访问不可用和异常响应等路径增加测试；
- 更新 README 的平台表格和必要的第三方声明。

如果平台只能依赖登录态工作，请先提交设计 Issue，说明为什么无法匿名访问；不要直接把账号或 Cookie 支持合入项目。

## 许可证

提交到 Reco Box 自有代码区域的贡献将按仓库的 MIT License 发布。第三方代码仍保留其原始许可证和版权声明。
