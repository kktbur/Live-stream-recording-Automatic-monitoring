# Reco Box 0.1.4

这是针对 0.1.3 的安全与发布整洁修订版，适用于 Windows x64。

## 变化

- 披露锁定上游解析器默认关闭 TLS 证书校验的兼容性风险。
- PyInstaller 的 EXE 与 COLLECT 均关闭 UPX，降低未签名构建的误报概率。
- Node.js 改为可选自检项，不再阻断当前公开平台的核心自检。
- 清理源 logo 中无业务用途的 Adobe XMP 元数据。
- 在审计文档中记录与官方 DouyinLiveRecorder v4.0.7 的 20 个文件逐一核对结果。

## 兼容性边界

本版本保留上游解析器当前的 TLS 兼容行为，尚未直接恢复证书校验。后续会
逐个平台测试，确认不会破坏解析后再收紧默认策略。

Node.js 不是当前公开平台的必需依赖；仅在启用依赖 JavaScript 签名的上游
平台时需要。
