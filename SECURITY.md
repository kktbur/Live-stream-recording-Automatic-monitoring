# Security Policy

Reco Box 处理直播地址解析、网络请求、FFmpeg 子进程、本地数据库和第三方解析器。请负责任地报告可能影响用户、文件或网络安全的问题。

## 支持范围

安全修复以最新 Release 和最新 `main` 分支为目标。旧版本可能不会单独回补；报告问题时请注明受影响版本和 Windows 版本。

## 私密报告安全问题

优先使用仓库 **Security** 页面中的 **Report a vulnerability**（如果该入口可见）。如果入口不可用，请创建一个标题为 `[Security Contact Request]` 的公开 Issue，只说明需要私密报告安全问题，不要公开技术细节、利用代码、凭据或个人数据；维护者会先建立私密沟通方式。

报告建议包含：

- 受影响版本、文件或功能；
- 风险和潜在影响；
- 最小复现步骤或已脱敏的验证材料；
- 建议修复方式（如有）；
- 是否已在其他地方公开。

请不要提交真实 Cookie、访问令牌、App Secret、代理密码、私人直播地址、临时签名流地址或录制内容。

## 适合报告的问题

- 任意代码执行、命令注入或不安全的子进程参数；
- 路径穿越、任意文件覆盖或删除；
- 本地数据库、日志或配置意外泄露敏感数据；
- 真实凭据进入源码、安装包、日志或 Git 历史；
- TLS、代理或重定向处理产生了超出已披露范围的可利用风险；
- 安装包、更新机制或依赖供应链可被篡改；
- 第三方解析器引入的高影响安全问题。

普通平台解析失效、直播间改版和录制兼容性问题请按 [CONTRIBUTING.md](CONTRIBUTING.md) 提交普通 Issue。

## 已知安全边界

锁定的上游解析器目前对主要异步平台请求默认关闭 TLS 证书校验，详见 [README.md](README.md)、[PRIVACY.md](PRIVACY.md) 和 [OPEN_SOURCE_AUDIT.md](OPEN_SOURCE_AUDIT.md)。仅重复这一已披露行为、但没有新增影响或利用证据的报告，不会作为新的未披露漏洞处理；能够证明额外影响或提出兼容修复方案的报告仍然欢迎。

上游公开代码中可能包含用于模拟公开网页请求的 App ID、请求头和公开客户端常量。公开标识符本身不等于 App Secret 或用户凭据；如果发现配套密钥、有效令牌或私人配置，请按安全问题报告。

## 处理预期

维护者会尽量在 7 天内确认收到报告，并在 14 天内给出初步判断。复杂的平台兼容问题可能需要更长时间。项目目前不提供漏洞赏金计划，也不承诺固定修复期限。

请在修复发布或维护者确认可以公开前，避免披露漏洞细节。
