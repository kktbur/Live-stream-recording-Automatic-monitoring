# PR-03 Local Maintenance Record

- Status: LOCAL, stacked on PR-02; final independent review passed; remote PR-03 CI verification pending
- Target repository: `kktbur/Live-stream-recording-Automatic-monitoring`
- Base for this local change: PR-02 commit `4e1481c3da610464fa3e8db37620d1d8fbfa841b`
- Working branch: `codex/0.2.1-03-uv-ci`
- Version: current product version `0.2.0`; PR-03 maintenance target `0.2.1`;
  no product-version bump in this CI-only change.
- Final branch-tip commit and remote PR metadata are recorded in the outer
  PR-03 worklog and evidence after publication; this repository-local record
  intentionally avoids duplicating a moving documentation commit.

## Scope

Migrate the Windows GitHub Actions job from manually creating a virtual
environment with pip to the locked uv project workflow:

- install uv with `astral-sh/setup-uv@v10.0.1` and enable its cache;
- fail early with `uv lock --check` when project metadata and `uv.lock` drift;
- install the existing development extra with `uv sync --locked --extra dev`;
- run the version reader, Ruff, and pytest through `uv run --locked`.

The `--extra dev` flag preserves the repository's existing
`pip install --editable ".[dev]"` behavior because development tools are stored
under `[project.optional-dependencies].dev`. Action commit-SHA pinning belongs
to the separate PR-04 task. Product code, packaging scripts, and runtime
assets are outside this PR. The CI metadata test uses the dev-only PyYAML
dependency to parse the workflow rather than maintaining a partial YAML parser.

## Backup and recovery

PR-02 remains an independent local and remote recovery point. The PR-03 branch
is isolated from the PR-02 branch and may contain implementation plus
documentation-only finalization commits. If CI or owner
acceptance fails, do not merge or publish further changes; revert this branch
to the PR-02 base and retain the worklog and evidence.

## Acceptance gate

The CI workflow must contain `uv lock --check`, `uv sync --locked`, and locked
`uv run` commands, while removing the CI-local `python -m venv` and `pip install`
path. Local static tests and Ruff must pass. The full local suite may
still report the known missing FFmpeg runtime prerequisite. A remote stacked PR
is only ready after independent Standards/Spec review and the recorded CI
result.
